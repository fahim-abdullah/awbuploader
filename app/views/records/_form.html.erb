<%= form_with(model: record, local: true, class: 'form-horizontal') do |form| %>
  <% if record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(record.errors.count, "error") %> prohibited this record from being saved:</h2>

      <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= form.text_field :prefix, :required => true, class: 'awbField col-2', placeholder: 'Prefix', maxlength: 3, minlength: 3 %>
    <%= form.text_field :awbnum, :required => true, class: 'awbField col-8', placeholder: 'AWB Number', maxlength: 8, minlength: 8 %>
    <%= form.file_field :doc, :required => true, class: 'uploadBtn', :accept => "image/*" %>
    <br>
    <%= form.submit 'Submit', :class => 'submit' %>

<% end %>
